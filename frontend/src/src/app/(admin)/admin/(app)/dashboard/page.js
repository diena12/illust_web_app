import Header from '@/app/(admin)/admin/(app)/Header'

export const metadata = {
    title: 'Laravel - Dashboard',
}

const Dashboard = () => {
    return (
        <>
            <Header title="Dashboard" />
            
        </>
    )
}

export default Dashboard